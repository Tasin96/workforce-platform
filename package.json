{
  "name": "workforce-platform",
  "version": "1.0.0",
  "private": true,
  "description": "On-demand workforce management platform by Tasin Islam",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js",
    "seed": "node backend/utils/seed.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Tasin Islam"
}
